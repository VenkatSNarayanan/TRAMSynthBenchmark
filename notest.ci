mainmodule test {
  readonly CProxy_main mainProxy;
  mainchare main {
    entry main(CkArgMsg*);
    entry [reductiontarget] void done(int);
  };
  array [1D] engine {
    entry engine();
    entry engine(int,int,int);
    entry void simulate();
    entry void ping(msg);
  };
};

